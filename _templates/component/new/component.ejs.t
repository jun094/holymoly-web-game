---
to: "<%= locals.route ? `${route}/${name}/${name}.js` : `src/components/${name}/${name}.js` %>"
---
import styles from "./<%=Name%>.module.scss"

function <%=Name%>() {
  return(

  )
}

export default <%=Name%>